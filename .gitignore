# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environments
venv/
.venv/
env/
ENV/

# Env files
.env
.env.*
!.env.example

# devagent local config (machine-specific paths/device_id)
devagent/projects.json

# demo-site is its OWN independent git repo (the devagent's live test
# target), not a submodule of this one — nesting its .git under this repo
# would make git track it as a broken gitlink. Keep it out entirely.
/demo-site/

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Expo / React Native
.expo/
.expo-shared/
dist/
web-build/
*.orig.*

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# my things  
.claude/
aim.md
docs/
